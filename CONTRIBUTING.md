# コントリビューションガイドライン

施設データの追加・修正にご協力いただける方を歓迎します。

## データの正は YAML

施設データは `data/facilities/` 以下のYAMLファイルで管理しています。

| ファイル | 内容 |
| --- | --- |
| `data/facilities/concerthall.yaml` | コンサートホール |
| `data/facilities/practice.yaml` | 練習場 |

サイトが読むJSONは、ここからビルド時に生成しています（`src/data/` 以下。コミット対象外）。
**JSONを直接編集しても次のビルドで消えます。**

## 編集の手順

```bash
npm install
npm run data     # YAML→JSON変換。データ検査もここで走る
npm run check    # lint → 型チェック → 整形チェック → テスト
npm run dev      # 表示を確認する
```

`npm run data` は変換の前にデータ検査（`scripts/validate.mjs`）を回します。
**エラーがあるとビルドが止まります。** 警告は種類ごとにまとめて表示され、ビルドは続きます
（全件見るには `node scripts/build-data.mjs --verbose`）。

## 書くときに気をつけること

スキーマの全体と判断基準は [CLAUDE.md](./CLAUDE.md) の「Data Schema」「記述ルール」にあります。
とくに間違えやすいのは次の3点です。

- **1レコード = 1施設**。同じ施設に複数のホール・練習室があるときは行を分けず `部屋:` 配列に入れる
- **設備の3値**。`true`＝あり、`false`＝なし、**キーごと省略＝未調査**。
  「調べていない」を `false` と書かない（実際には設備がある施設が絞り込みから消えます）
- **出典を必ず残す**。値の裏が取れたURLと確認日を `出典:` に書く。
  出典の無いデータは未検証として扱い、詳細ページに「出典・確認日は未記録です」と表示されます

**`ID` は一度振ったら変更しないでください。** `/concert/:id` `/practice/:id` としてURLに使うため、
振り直すと外部からのリンクが切れます。新規は「そのファイルの最大ID + 10」を振ります。

## 提案の送り方

Pull Request を歓迎します。施設の追加・修正では、出典URLと確認日を添えてください。

送っていただいた施設データは [CC BY 4.0](./data/LICENSE.md)、コードは [MIT](./LICENSE) で公開されます。

リポジトリへの編集権限のリクエストは管理人までご連絡ください。
なお、現時点では管理人と面識があり、連絡が取れる方に限り権限をお渡ししています。
