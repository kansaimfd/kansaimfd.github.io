# src/datasets

変換後JSONの読み込み口。**データセットごとにファイルを分けているのは、
バンドルを分割できるようにするため**。

1つのモジュールが3つのJSONをまとめて import すると、そのモジュールに触れた
時点で全データが同じチャンクに入る。入口のコンサートホール一覧しか見ない
利用者にも、練習場239KBと詳細ページ用297KBが配られてしまう。

ページからは必要なデータセットだけを import すること。
